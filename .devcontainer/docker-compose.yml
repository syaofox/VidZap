version: '3.8'

services:
  app:
    build: .
    image: vidzap
    container_name: vidzap
    volumes:
      - ..:/vidzap:cached
      - /home/syaofox/.agents:/home/vscode/.agents
    command: sleep infinity

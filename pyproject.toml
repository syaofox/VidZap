[project]
name = "vidzap"
version = "0.4.0"
description = "纯 Python 视频下载工具（NiceGUI + yt-dlp）"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "nicegui==3.9.0",
    "fastapi",
    "uvicorn",
    "yt-dlp>=2026.7.0",
    "pydantic",
    "python-multipart",
    "playwright",
    "httpx",
    "playwright-stealth>=2.0.2",
]

[project.optional-dependencies]
dev = ["ruff", "mypy"]

[tool.ruff]
line-length = 100
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]

[dependency-groups]
dev = [
    "mypy>=1.19.1",
    "pytest>=9.1.1",
    "pytest-asyncio>=1.4.0",
    "pytest-cov>=7.1.0",
    "ruff>=0.15.7",
]

[tool.mypy]
python_version = "3.13"
explicit_package_bases = true
warn_return_any = true
warn_unused_configs = true
ignore_missing_imports = true
disallow_untyped_defs = false
